# Supabase Functions

The current implementation uses Next.js route handlers as the trusted backend-for-frontend. This folder is reserved for future Supabase Edge Functions if specific low-latency or scheduled workloads justify moving logic closer to the database.
