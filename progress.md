Original prompt: Build a production-minded Telegram Mini App game platform with a premium portal, centralized design system, secure Telegram auth, server-authoritative official race results, admin/ops visibility, Supabase migrations/RLS, and one polished single-player racing POC with deterministic CPU opponents.

## Progress log

- 2026-03-09: Initialized empty workspace, verified local Node/pnpm/npm toolchain, verified current stable package lines for Next/React/Tailwind/Phaser/Supabase, and locked the architecture plan.
- 2026-03-09: Chosen implementation stack remains the preferred stack: Next.js 16, React 19, Tailwind 4, Phaser 3.90, Supabase Postgres migrations, Vercel, GitHub Actions, Pino, Vitest, Playwright, pnpm workspace, turbo.
- 2026-03-09: POC decisions locked: XP + Coins rewards, Telegram allowlist admin auth, auto-accelerate controls, tilted top-down voxel-inspired racer presentation.
- 2026-03-09: Created the repository root skeleton and began writing shared workspace configuration.
- 2026-03-09: Implemented shared packages for design tokens, theme manifests, UI primitives, telemetry, deterministic game core, and Phaser racer rendering controller.
- 2026-03-09: Implemented the Next.js Mini App shell, Telegram auth route, session cookies, memory/postgres store abstractions, portal pages, play screen, leaderboard, profile, ops page, client error reporting, and bot webhook entrypoint.
- 2026-03-09: Added repository docs, ADRs, SQL migration and seed files, bot/dev helper scripts, GitHub Actions CI, unit tests, Playwright smoke coverage, and a dev-auth redirect route for automation-friendly local Mini App verification.
- 2026-03-09: Validation completed successfully with `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm --filter @telegramplay/web test:e2e`, and the `$develop-web-game` browser client. Latest browser client artifact: `output/web-game/shot-0.png` with matching finished race state in `output/web-game/state-0.json`.
- 2026-03-09: Began in-place multi-game refactor. Extracted racer simulation into `packages/game-racer-core`, repurposed `packages/game-core` as generic platform contracts, introduced a game registry and generic `/api/games/[gameSlug]/...` routes, and removed the racer-specific public API endpoints.
- 2026-03-09: Refactored profile/contracts further so `game_profiles` are generic progression records while racer-only counters/best-result data live behind `racer_player_stats` and module-specific stat builders. Updated the shell, tests, and route consumers to the new game-module surface.
- 2026-03-09: Added the in-place multigame schema migration, updated seeds/docs/ADRs/README/AGENTS to the generic session model, and revalidated the refactor with `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm --filter @telegramplay/web test:e2e`, plus a fresh `$develop-web-game` run through `dev-auth` into `/games/racer-poc/play`. Latest artifacts remain in `output/web-game/shot-0.png` and `output/web-game/state-0.json`.
- 2026-03-09: Fixed Telegram Mini App bootstrap on mobile by loading the official Telegram WebApp bridge script in the root layout and making auth bootstrap wait briefly for `initData` instead of failing immediately. Added a focused unit test file for delayed bridge/init-data resolution.
- 2026-03-09: Tightened the play-page mobile presentation: shorter play header copy, smaller mobile-first racer canvas height, and an explicit "Preparing official session..." overlay while the authoritative session is being allocated.
- 2026-03-09: Cleared noisy client-side warnings by suppressing expected root-layout hydration drift from Telegram-injected viewport styles and adding `apps/web/app/icon.svg` to stop favicon 404s. Revalidated with `pnpm --filter @telegramplay/web typecheck`, `pnpm --filter @telegramplay/web lint`, `pnpm --filter @telegramplay/web test`, `$develop-web-game`, and a narrow-viewport Playwright capture showing the racer canvas visible on mobile.
- 2026-03-09: Fixed a production-only Postgres bug where Supabase `jsonb` columns were being returned as strings, which broke `gameSession.config.payload.track` on mobile. Added `parseDbJson`, a regression test, and verified the play route against the real Postgres-backed store before redeploying.
- 2026-03-09: Refactored `/games/[gameSlug]/play` into an immersive mobile-first Telegram play shell. Play routes now hide global nav, lock page scrolling, consume safe-area insets, pin touch controls to the bottom edge, expose instructions through an in-surface help button, and enable Telegram viewport expansion with swipe suppression.
- 2026-03-09: Added a generic authenticated-player wait helper so game-session creation no longer races Telegram auth bootstrap on direct entry to `/play`. Covered it with unit tests and updated the smoke flow to enter via `dev-auth` before exercising the official result lifecycle.
- 2026-03-09: Revalidated the mobile-first play shell with `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm --filter @telegramplay/web test:e2e`, `pnpm build`, and a real 390x844 Playwright capture. Latest artifact: `output/playwright/mobile-play-route.png`.
- 2026-03-09: Tightened the immersive HUD again after real Telegram feedback. The title panel is now much smaller, the metric row is a compact 4-up strip, the normal-play `Official Run` card is hidden, and the track projection is pushed lower so the race stays visible under the top HUD. Latest artifact: `output/playwright/mobile-play-route-compact.png`.
- 2026-03-09: Fixed the finish-submit payload to round client summary integers before server validation, replaced raw Zod dumps with a stable `invalid_submission_payload` server error, and reworked the track projection math so the track is centered in the free viewport between the compact top HUD and bottom controls. Latest artifact: `output/playwright/mobile-play-route-centered.png`.
- 2026-03-09: Reduced the mobile HUD once more after device feedback. The play title chip, help/restart buttons, and Lap/Place/Speed/Time strip are all smaller, and the Phaser projection now reserves more top space so the track sits lower and more centrally between HUD and controls. Revalidated with `pnpm --filter @telegramplay/web typecheck`, `pnpm --filter @telegramplay/web test`, `pnpm --filter @telegramplay/web test:e2e`, `pnpm build`, and a fresh 390x844 capture: `output/playwright/mobile-play-route-lowered.png`.
